[project]
name = "pythermondt"
version = "0.0.2"
description = "A package to manipulate thermographic data for NDT purposes."
authors = [{name = "Julian Grimm", email = "julian.grimm@fh-wels.at"}]
license = {file = "LICENSE"}
readme = "README.md"

[tool.setuptools.packages.find]
where = ["./src"]

[tool.pytest.ini_options]
addopts = "-vv"
testpaths = [
    "./tests",
]
pythonpath = [
    "./src",
]